# No Edge Functions are deployed for LastBag.
#
# All server-side logic (notifications, webhooks, cron) lives in Next.js API
# routes under backend/app/api/. See supabase/functions/README.md.
#
# The payment-* functions were removed along with the in-app payment feature —
# v1 partners pay manually and an admin marks the invoice paid.
